# OWNER_AUTHORIZED_ART_CLEARANCE — P0-06 Gomoku Final Art v1

基于项目已确认的 M0 North Star，本批次原创 Ghost-native source masters、本地派生 WebP、provenance、稳定 ID/version/SHA、机器技术/视觉审查、fallback、feature flag 与一键回滚合同均已冻结。

因此 `G-02-GOMOKU-FINAL-ART-V1`（18 source masters / 36 runtime variants）取得 `OWNER_AUTHORIZED_ART_CLEARANCE`，可进入可逆的本地 default-on Runtime 候选。默认旗标为 `mg_art_gomoku_final_v1`；将其写为 `0` 即回滚至既有 Wave A/M0/Canvas/CSS/程序化 Ghost3D 链路。

清除范围只覆盖表现层。棋盘网格、落子合法性、当前回合、胜负/和棋、snapshot、Replay、AI、Authority、Reward、Protocol、聊天和持久化不读图。Manifest、路径、加载、解码或 late-result 失败均 fail-closed 并保留旧 fallback。

本清除不构成人工清稿、Reviewer B、IP/法律意见、逐资产 Golden Set、设备/第二浏览器/真实网络、生产数据或发布批准。`OPTIONAL_ADVISORY_EVIDENCE` 未执行；It does not constitute human, legal, production, or release approval. 外部 `EXTERNAL_REFERENCE_ONLY / blocked-license` 素材不在清除范围内，仍永久禁止进入生成输入、Runtime 或发布。
