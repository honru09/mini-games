# OWNER_AUTHORIZED_ART_CLEARANCE — P0-02 Platform Scenes v1

生产单元：`P0-02`  
稳定 Runtime family：`P-PLATFORM-SCENES-V1`  
Artwork version：`1`  
状态：`OWNER_AUTHORIZED_ART_CLEARANCE`

## 清除结论

项目所有者已确认 M0 North Star 与 Ghost-native 视觉方向。P0-02 的 48 个项目自有 SVG 母层、80 个 WebP Runtime 变体和 4 张审查板已具备稳定 Asset ID、版本、SHA-256、provenance、机器技术/视觉/相似风险审查、Manifest allowlist、feature flags、decode-before-activate、fallback 与一键回滚合同，因此取得原创资产的所有者清除，可作为可逆 default-on 本地 Runtime 与未来发布候选。

## 允许范围

- 允许 `mg_art_platform_scenes_v1` 与四个路由子闸在本地 default-on 生效。
- 允许按 route / theme / viewport 选择 layered、static 或 poster 变体。
- 允许在不修改规则、协议、经济、社交或持久化事实的前提下进行页面表现与性能回归。

## 不构成的结论

- 不构成独立 Reviewer B、人工可编辑清稿、IP/法律意见或逐资产 Golden Set PASS。
- 不构成第二浏览器、Android、iPhone、Tablet、真实网络、真实 Supabase 或生产验收。
- 不触发 commit、push、Pages、Render、生产数据写入或发布；发布仍需当前用户明确命令。

## 回滚链

```text
P-PLATFORM-SCENES-V1 layered runtime
  -> 同族 static / poster
  -> 既有 CSS cloud/star ambient scene
  -> P-001 Ghost Mark / DOM 内容
```

外部 `EXTERNAL_REFERENCE_ONLY / blocked-license` 素材永久不在清除范围内：不得复制、描摹、裁切、换色、作为生成输入、接入 Runtime 或发布。

