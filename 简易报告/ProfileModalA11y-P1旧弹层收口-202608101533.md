# Profile Modal A11y P1 旧弹层收口

时间：2026-08-10 15:33（Asia/Tokyo）

## 完成内容

- Profile 编辑器和成就弹层接入共享可访问 dialog helper 与 owner 滚动锁。
- 昵称输入框/关闭按钮获得初始焦点；Tab/Shift+Tab 循环、Escape、背景、保存、取消、关闭全部走同一幂等关闭路径。
- 关闭后恢复发起控件焦点；迟到事件不重复释放滚动锁。
- 两类弹层增加 44px 控件、窄屏安全高度与内部滚动。

## 子 agent 与主审

- 第一位 Terra Max 完成 canonical source 侦察、冻结文件和 17 项红灯合同，并正确拒绝在错误文件里复制函数。
- 第二位 Terra Max 在主负责人批准真实所有权后完成两处最小实现和回归。
- 主负责人补充保存/取消/关闭显式动作断言，以及“成就弹层不得以内联桌面宽度压过手机 CSS”的动态断言；确认移除 460px 内联宽度。

## 验证

- 专项、三语言、DOM、Profile/Social、Profile Route 全部通过。
- 完整 `npm test`：122.5 秒通过。
- 双构建一致：953847 characters、967961 physical bytes、SHA-256 `B07BD0597D0B9834FB2C2C084ED7FD9AAE6ABC6B85D42766BE91DBFFA9C65B96`。

## 边界

- 未修改资料字段、成就判定、协议、服务端、持久化、奖励、商城价格、Supabase、规则、AI、Replay 或美术。
- 第二浏览器、Android/iPhone/Tablet、真实网络、localhost 可见复核和线上发布仍未执行。

下一条独立本地主线：收藏稀有度不可变展示目录；稀有度不从价格推断。
