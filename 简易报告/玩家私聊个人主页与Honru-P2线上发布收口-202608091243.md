# 玩家私聊、个人主页与 Honru P2 线上发布收口

时间：2026-08-09 12:43（Asia/Tokyo）

## 已上线

- 发布提交：`94ae9771745c08ae33aac800a06575400a4650f4`；功能分支与 `main` 均已推送，`main` 从回滚点 `c0140c9` 快进。
- Honru P2 独立提交 `551b91a` 完整保留并随本次发布上线；九状态双旗标继续默认关闭，v1 fallback 不变。
- Render 部署 `dep-d9rvbfv10e5c738tnclg` 已 `live`；GitHub Pages 工作流 `31292922140` 成功。
- Render 与 Pages HTTP 均为 200，包含玩家聊天、深度 Profile 与 `direct-chat-v1`；临时账号 WS 建房、READY、落子、多人房、不满员开局和结算通过。

## 发布前验证

- 干净发布树 `npm run quality:gates` 通过。
- 完整 `npm test` 通过，耗时 104.3 秒。
- 暂存差异为 36 个预期文件，秘密扫描未发现 Render/DeepSeek/Supabase Key 或私钥。

## 未执行与边界

- 本轮可见浏览器控制解析到 Node 20.20.2，低于连接器所需 22.22.0；因此不能把 HTTP/DOM 自动化写成真实视觉矩阵通过。
- 未使用生产正式账号伪造好友关系；线上两正式好友 UI 私聊、离线留言、未读/已读仍待专用 QA 账号或 staging。
- 真实 Supabase 迁移、RLS、并发、备份/回滚，第二浏览器、Android、iPhone、Tablet、真实网络整形与 30 分钟会话仍未执行。
- Sticker/Honru 人工清稿、Reviewer B、IP Review、Golden Set 决议和默认开启审批仍未完成。

结论：代码、GitHub Pages 与 Render 发布完成并通过自动化/HTTP/WS 线上冒烟；项目 RC 仍因外部生产闸门保持 `BLOCKED`。
