# 技术优化主线 T5 坦克传输与预测收口

时间：2026-08-16 20:00（Asia/Tokyo）  
状态：`LOCAL_IMPLEMENTED / DEFAULT_OFF / NOT_RELEASED`

## 完成内容

- `TankSnapshotWireCodec` 与服务端 stream 按 session 生成 `tank-snapshot-delta-v2` keyframe/delta；Tank Authority 与游戏调用方始终只见完整 canonical `tank-authority-v1`。
- 缺 base、错 match、旧/重复/冲突 frame、serverTick 回退、未知字段、非有限数、非法长度全部 fail-closed；新旧客户端可同房，服务端开关默认 `0`。
- 观众延迟改为房间级最多 320 个共享 Timer，发送前复核 room/match/session/membership；离席、换局和 Authority 停止后旧帧失效。
- `TankPredictionAdapter` 最多保留 8 条未确认 control state，只预测本地 x/y/d；Authority tick/ack 仍是事实。调用方补有限路径扫掠，避免表现预测跨墙角；弹道、HP、墙体事实、击杀、排名、终局、Reward 和 Replay 不进入预测模块。

## 主审修正

- 修正 Codec 未进入浏览器构建图、同 tick 重复广播、断线 Session 重建 base、Projectile 仅顺序变化丢失、恢复后 tick 回退、旧 match v1 重置 decoder 与 queue-full 后无法可信 rebase。
- 新增真实 WebSocket default-off 回归：能力客户端声明 v2 时，只要服务端开关为 `0`，能力客户端和旧客户端都只收到 v1。
- 独立 Sol Max 审计发现路径穿角潜在风险与协议文档漂移；已补路径扫掠、README/协议注册表、台账和报告同步。

## 验证与构建

- `npm run test:technical-optimization-t5` 通过；覆盖 codec、stream、client、prediction、混合 v1/v2、350ms 延迟观众离席、重连、协议、Authority 与受控传输预检。
- 修正后的当前构建：1,856,038 characters / 1,870,581 bytes / SHA-256 `16A5B3AD5D1BB20BA427471F9D5DDE73C75A044C4531804CA7482812CBAC5D7C`。
- 第二浏览器、真机、真实网络与线上最新版本仍未执行；T5 完成后主线已进入 T6。
