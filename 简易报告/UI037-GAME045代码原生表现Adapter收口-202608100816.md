# UI-037 / GAME-045 代码原生表现 Adapter 收口简易报告（2026-08-10 08:16）

## 当前节点

前置 Social Match capability 修正、Player Character P0、ART-036 source-only、ECO-029 contract-only 已核对。当前批没有发布线上，工作树仍为本地收口状态。

## 本次完成

- 新增 `MonopolyCharacterPresentation.project(input)`：集中裁剪 `player-character-v1` 公开投影，区分权威位置与视觉位置，计算回合/移动/机会卡/购买/拍卖/破产/结算状态、朝向、reduced-motion 与固定 `code-fallback`。
- 新增 `MonopolyPresentationAdapter.create()`：校验 `monopoly-rule-v2` 的 `matchId/revision/stateHash/state`，只接受连续且严格匹配的 `transition.move` 生成有限步进计划；首帧、重连、观战、乱序、重复、跳 revision、异常资源和跨 realm 输入均安全 snap/fallback。
- 客户端只转交服务器已有根级 `transition`，没有修改服务端 wire、Rule Core、奖励、Replay、AI、商城、Supabase 或用户数据；Monopoly 仍使用现有 CSS/DOM `♟/🚗` marker。
- 修复 ECO-029 `rememberRequest/hasRequest` 注入目录丢失 owned/equipped 的边界，并新增 Social Match 客户端同连接 capability 生命周期回归。
- 文档、台账、项目状态、AGENTS/README/WHITEPAPER、分类进度报告与三份日志已同步；ART-036 G-14/G-15 仍是 `reference-only`，未进入 `public/assets` 或 Manifest。

## 验收证据

- `npm test`：ALL_PASS，114.7 秒；包含完整 i18n、DOM、六款游戏、协议、安全、重连、E2E、Supabase fake adapter、关闭测试。
- `node qa/monopoly-character-presentation.js`、`node qa/monopoly-presentation-adapter.js`、`node qa/social-match-client-lifecycle.js`：全部通过。
- `npm run test:progress-ledger`：233 项需求、7 份报告、60 个来源、235 条依赖边全部通过。
- `npm run quality:gates`：`QUALITY_GATES_FAST_ALL_PASS`。
- `node scripts/asset-library-audit.js`、`node qa/asset-manifest-v2.js`、`git diff --check`：通过。
- 最终构建：`public/index.html` 896153 bytes，SHA-256 `870B1A520CD59B5AC67034A1CA0DE1EA9B3BFC3213280558470E791EC4F91976`；构建漂移检查通过。

## 尚未完成 / 外部闸门

- UI-037 完整进入、回合、骰子、事件、支付、交易（当前规则未提供）、拍卖、断线恢复和结算 UI 状态矩阵尚未完成。
- ART-036 人工清稿、Reviewer B、IP Similarity Review、Golden Set、获批 runtime renderer 与真实设备/浏览器视觉验收未完成；方向板严禁进运行时。
- ECO-029 正式角色 catalog、购买/装备 RPC、RLS、并发、备份恢复/回滚和商城 UI 未完成。
- in-app Browser 的 localhost 访问仍被已保存权限拒绝；第二浏览器、Android/iPhone/Tablet、真实网络整形尚未形成证据。
- 本批未 commit、未 push、未部署；等待用户明确“输出线上/推送/部署”。
