# TECH-050 中文任务收口简报合同（2026-08-13 21:55）

## 一句话结论

TECH-050 已完成本地治理收口：以后每个主线批次都有统一的中文交接格式，且会明确区分本地实现、外部验收和线上发布。当前线上仍冻结，未发布。

## 做了什么

- 新增《任务收口简报合同》，固定九段结构：结论、做了什么、用户可见变化、未完成项、验证、风险/下一步、发布状态和追溯入口。
- 新增可复制的中文简报模板，统一使用 `主题名-YYYYMMDDHHmm.md` 时间后缀。
- 新增专项 QA，检查模板字段、时间后缀、本地发布冻结边界，以及 API Key、Render/Supabase token、session token 等敏感信息不进入简报。
- 把 TECH-050 从 `planned` 更新为 `implemented`，并把合同与 active task 接入需求追溯。

## 用户现在能看到什么

- 以后每次任务结束，都能在一份短报告里直接看到“做了什么、现在能看到什么、还缺什么、下一步是什么”。
- 详细技术报告、七份分类进度报告和三份中文增/改/删日志继续保留，各自承担不同用途；没有删除历史内容。
- 这次本身是治理改动，不改变网页、游戏、账号、聊天、商城或美术运行时。

## 还没做什么

- `NOT_EXECUTED`：最新本地浏览器可见复核、第二桌面浏览器、Android/iPhone/Tablet、真实网络整形、visible reduced-motion 和低端 FPS。
- `BLOCKED`：真实 Supabase 迁移/RLS/备份恢复/回滚/多实例，以及 Honru/Sticker/Emoji/G Coins 的人工清稿、Reviewer B、IP Review、Golden Set。
- 本批不负责修复上述外部门禁，也不把静态 QA 描述为视觉或真机验收。

## 验证

- `npm run test:brief-report`：PASS。
- `npm run reports:progress`：PASS（7 份报告，`changed=0`）。
- `node qa/progress-ledger.js`：PASS（242 项、72 个来源、267 条依赖边）。
- `npm run quality:gates`：PASS（含简报合同 Gate）。
- 完整 `npm test`：PASS（165.9 秒）。
- `node scripts/build.js`：PASS；`public/index.html` 1,353,257 字符 / 1,367,874 字节，SHA-256 `2E466A3B59CEC8B7B1323DC6FD61375395E2497BE8C837C0D01A789D02731E93`。
- `git diff --check`：PASS。

## 风险与下一步

- 风险：简报是交接层，不能替代真实浏览器、设备、数据库或人工美术证据；生成进度报告仍只能由台账生成器覆盖。
- 下一步：继续 CLOSE 表现层收口；之后按总指挥顺序进入 PROVE，再 DATA、ART、PARITY、LOOP、COMMUNITY。若用户明确要求发布，才单独执行提交、推送和部署。

## 发布状态

- `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`
- 未提交、未推送、未触发 GitHub Pages 或 Render；线上保持 `bd49e6d` / `da3d05c` 基线。

## 追溯入口

- 需求：`TECH-050`。
- 合同：`requirements/BRIEF_REPORT_CONTRACT.md`。
- 模板：`简易报告/模板-任务收口简报.md`。
- QA：`qa/brief-report-contract.js`、`npm run test:brief-report`。
- active task：`requirements/active/brief-report-contract-p1-20260813/`。
