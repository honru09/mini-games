# Home Identity P1 当前身份条收口｜2026-08-10 17:10

## 结果

Home Identity P1 已在现有 `#home-engagement-pulse` 内完成本地只读纵切，没有新增首页卡、路由、协议或持久化。

- 正式账号显示 56px 已装备头像/头像框/特效组合、raw 昵称与三语本地化 `Lv.N`。
- 继续复用本人收藏编目 X/Y，以及既有 Profile、Chat、Shop 导航。
- 访客/未登录在读取 `owned` 或调用 `avatarStageNode`/`nameFxNode` 前短路；重渲染会清空旧身份 DOM。
- catalog 或 owned 异常时保持只读身份条和原有脉冲，不抛错。
- 不显示 coins、XP、价格、owned ID、商品名、购买记录、好友明细、playerCharacter/gameCosmetics 字段或未审批图片；不新增网络、购买、Profile 保存 mutation。

## Terra Max 与主审

Terra Max 先冻结 recon/requirement/contract/ownership/plan，新增专项红测；实现仅修改 Home Shell、模板、三份 locale、测试注册和构建产物。红测在实现前确认 8 项预期失败。主审复核动态 getter 后确认正式账号可读、访客 `owned` 不读、身份 helper 不调用、旧私有 DOM 清理和 catalog 降级均有覆盖。

## 验证

- Home Identity 专项、Home P0/P1、Collection Rarity、Victory Mastery、Identity Preview、Profile Route：通过。
- `npm run test:i18n`、`node qa/dom-smoke.js`、`node qa/ui-responsive-contract.js`、`node qa/ghost-shell-contract.js`、`npm run pretest`、`npm run quality:gates`：通过。
- 完整 `npm test`：通过，120.7 秒。
- 双构建一致：971303 characters / 985572 physical bytes；SHA-256 `963DEAEFC5B46621ACCE9B713444D3F3B7F5DC41C775990CD87BE36E501D69FF`。

## 未完成与边界

in-app Browser localhost 可见复核仍被用户保存权限阻断；第二浏览器、Android/iPhone/Tablet、真实网络整形、可见 reduced-motion 和人工美术/IP/Golden Set 未执行。G Coins 命名/图标/获得路径、角色服装/商城/背景仍受独立 ART/ECO 门禁。下一主线只可冻结为“返回当前仍有效的同实例 Game Stage”，不承诺跨设备或跨重启恢复。

本批未提交、未推送、未部署。
