# 玩家私聊与个人主页深度优化本地收口

时间：2026-08-09 10:31（Asia/Tokyo）

## 结论

本轮“先做玩家之间聊天，再深度优化个人主页”已经完成源码、协议、数据库迁移合同、三语言、响应式与全量自动化，本地状态为 `VERIFIED_LOCAL`。`npm run quality:gates` 与完整 `npm test` 均通过。

当前不能宣称历史要求已经全部完成，也不能写 `PRODUCTION_READY`：本轮尚未推送/部署，真实 Supabase 迁移与真实浏览器/设备/网络验收仍未执行；Sticker Cartoon Golden Set 的人工清稿、双人 IP Review 与人工决议也仍未完成。

## 玩家私聊已完成

- Chat 默认进入“玩家消息”，Honru 保留为独立 `#/chat?view=honru` 子页。
- 正式好友一对一纯文本消息；桌面/平板双栏，手机会话列表/线程主从切换。
- 在线发送、离线留言、历史分页、会话摘要、全局未读、账号级已读、多 session 同步和重连恢复。
- 服务端签发 sender、conversation、message ID、十进制字符串 seq 与时间；`clientMessageId` 幂等，相同 ID 冲突拒绝。
- 访客、非好友、任一方向 Block、跨会话历史、伪造 sender、空/超长正文、非法游标和发送频控均由服务端拒绝。
- 旧 token 被五 token 上限淘汰后不再收到主动推送；这个审计同时修复了旧 Social 推送只检查 `uid + alive` 的安全缺口。
- 正文 NFC/控制符净化，最多 500 Unicode/2000 UTF-8 bytes，只用纯文本渲染，不进入 Profile、Leaderboard、Replay、Analytics、普通日志或 localStorage。
- 本地回退保留 90 天/每会话 500/全局 50,000；Supabase 新增消息/已读表、RLS、service-only 发送/已读/分页 RPC，启用 Supabase 时远端成功持久化后才回执。

## 个人主页已完成

- 装备背景身份 Hero、头像/相框/特效/闪名、签名、地区、Presence、称号与精选展示。
- 服务端 `xpProgress` 语义进度条；等级、局数、胜场、胜率、当前/最佳连胜、成就和余额。
- 固定六款游戏卡，展示真实局数、胜场和派生胜率；零场次显示 `—`，不伪造负场或平局。
- 八项真实成就墙、服务端每日任务及领取、好友/在线好友、最近一起玩、收藏数量与本人参与且可分享的近 7 日回放。
- 修复旧“成长与任务”按钮错开 Profile 弹层的薄壳设计；退出降为页底安全操作。
- 修复账号切换时 Social、任务、Replay 与 Chat 内存缓存残留风险。
- 高级个人背景复用既有生命周期并保持不随昼夜主题改变。

## 验证

- `qa/player-chat-contract.js`：通过。
- `qa/player-chat-online.js`：15 项通过，含离线、幂等、已读、Block、访客、越权和 token 淘汰。
- `qa/profile-route-contract.js`：10 项通过。
- Social、Security、Reconnect、Supabase Schema/Fake Adapter、Daily、Replay、Cosmetic、i18n、DOM、Responsive：全部通过。
- `npm run quality:gates`：`QUALITY_GATES_FAST_ALL_PASS`。
- `npm test`：通过，119.8 秒。

自动化证据：`requirements/active/player-chat-profile-p0-20260809/evidence/automated-verification-202608091031.json`。

## 浏览器连接器状态

连接器本身已成功连接到 Codex In-app Browser，当前不再是“内核资源路径错误”。真正阻断点是本机保存的安全权限禁止自动化访问 `127.0.0.1:8080`；按安全规则未绕过。因此本轮 Chat/Profile 视觉矩阵为 `NOT_EXECUTED`，详见同任务 `evidence/browser-visual-qa-202608091031.md`。

## 历史要求与剩余阶段

当前属于“平台核心社交与身份体验已本地验证、等待发布收口”阶段。剩余按四层管理：

1. 发布收口：把 Honru P2 与本轮 Chat/Profile 形成连续提交，推送 `main`，触发 GitHub Pages 与 Render，执行线上 HTTP/WS/Chat/Profile 冒烟。
2. 生产外部验收：真实 Supabase 迁移/RLS/并发/备份回滚、第二桌面浏览器、Android/iPhone/Tablet、真实网络整形与 30 分钟会话。
3. 美术主线：M0 Art Bible/Golden Set 人工清稿、Reviewer B/IP 决议，通过后再批量覆盖 Persona/Avatar/UI/五子棋/飞行棋及更多局内 Honru；现有 M0/P1/P2/旧风格/fallback 均保留。
4. 后续扩展：Tetris T-Spin/B2B/Combo/Perfect Clear、跨实例 Pub/Sub/指标/Reward/AI 单写者、高级观战、更多平台端。

其中第 1 层继续自动执行；第 2/3 层包含真实凭证、权限或人工评审，未满足前保持 `BLOCKED/NOT_EXECUTED`。
