# 沉浸式 Game Shell P0 本地收口

时间：2026-08-09 21:34（Asia/Tokyo）

终审修订：2026-08-09 21:59（Asia/Tokyo）

## 当前节点

`UI-014 / UI-015 / UI-016 / GAME-020` 已从待办升级为 `verified`。Game Shell P0 已完成本地实现与验收，状态为 `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`；下一主线是 `Social Match P0`。

## 已完成

- 六款游戏共用 fixed `100dvh` 全视口 Shell，冻结 `header / seats / arena / command / overlay` 五个插槽。
- 锁定页面滚轮、触摸滚动链、Space/方向键/Page/Home/End 默认滚动，同时不停止游戏事件传播。
- Seat、Arena、Command 保留内部滚动；手机单列、平板/桌面双列、低高度横屏紧凑双列。
- 进入/退出幂等，恢复 Hub 滚动和对应游戏卡焦点；状态失同步也清理残留 `game-active`。
- Tetris 七项操作在四档视口均不低于 44px；Seat 原生滚动槽隐藏，内部纵向滚动条细化。
- Rules、Victory Overlay 与 Reward Breakdown 统一具备命名 dialog、初始焦点、Tab 循环、Esc/背景/按钮关闭和焦点恢复。

## 主负责人浏览器审核与修正

- 修复从 AI 数量弹层进入后退出只聚焦 `body`、Hub 滚动归零的问题。
- 修复 1024×768 和 844×390 Tetris 控件仅 38px 的问题。
- 修复 Rules Modal 焦点留在背景按钮的问题。
- Terra Max 首轮终审发现 Victory/Reward 实际结算路径未复用可访问生命周期；二轮终审继续发现 Rules 缺 Tab 闭环及 Victory 重复创建/外部移除时的监听残留。现已统一为单实例、幂等关闭，并由 22 项动态 QA 覆盖；旧实现无写入回放真实失败 5 项。
- 修复 Shell 状态失同步时重复退出不能清理残留 class 的问题。
- 1440×900、1024×768、390×844、844×390 四档截图和交互证据见 `requirements/active/immersive-game-shell-p0-20260809/evidence/browser-qa.md`。

## 验证

- `npm run quality:gates`：通过。
- `npm test`：通过，189.2 秒；含新增弹层动态合同，联机 E2E、安全、重连、规则权威、AI、Supabase fake adapter 全部通过。
- `npm run test:progress-ledger`：208 项、7 份报告、58 个来源、168 条依赖边全部通过。
- 两次构建 SHA-256 一致：`4236BF12F501470A9684F80F7DF810F3CC994899482BEC46011ACABE22BE02DD`。
- 浏览器控制台 warning/error：0。

## 未执行与边界

- 未执行第二桌面浏览器、Android、iPhone、真实 Tablet、真实网络整形和浏览器 reduced-motion 媒体模拟。
- 未生成图片，未开启 Sticker/Honru 冻结资产。
- 未修改服务端、共享规则、WebSocket 协议、奖励、Replay 或 Supabase。
- 未 commit、未 push、未触发 GitHub Pages 或 Render；等待用户明确发布指令。
