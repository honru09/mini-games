# Honru P2 运行时融合与发布前收口

时间：2026-08-09 09:19（Asia/Tokyo）

## 本轮完成

- 九个 Honru 表情状态已从 `art-source` 投影为版本化 Alpha WebP，登记 `P-HONRU-STATES-V1`、逐文件 hash/bytes、512 KiB 组预算和 v1 fallback。
- 新状态只有 `mg_art_honru_states_v1=1` 与 `mg_art_honru_game_reactions_v1=1` 同时存在时启用，线上默认关闭。
- 签到、聊天思考/恢复、邀请等待、六款局内操作和真实 `win/loss/draw` 已接入纯表现层；观众不显示个人胜负。
- replay、页面隐藏、重开、销毁、离开游戏、Manifest 异常、decode 失败、加载中撤旗标、高频事件和终局优先级均有保护。
- 飞行棋已改为合法动作提交后反馈；象棋重绘保留 Honru 节点；规则、AI、联机、Replay、奖励、商城和 Supabase 均未新增 Honru 字段。
- 登录页黑夜模式单色 Logo 已显式反转，主题按钮同步图标、title 与 aria-label。

## 素材保留

没有覆盖或删除旧素材。Honru v1/v2、M0 Teacher/Avatar、P1 五子棋贴纸棋盘、五子棋旧木纹、俄罗斯方块旧纵切、六款大厅封面与程序化 fallback 均继续存在，可独立回滚。

## 验证

- `npm run quality:gates`：通过。
- 完整 `npm test`：通过，包含新增 33 项 Honru Runtime 合同。
- Manifest、素材库、DOM、三语言、AI、Gameplay、权限、奖励、重连、Supabase fake adapter 与联机 E2E：全部通过。
- 本地 30 组合浏览器矩阵：未执行。原因是 in-app Browser 的 localhost 权限被拒绝，安全规范禁止绕过；不能把静态测试冒充视觉通过。

## 当前阶段与剩余阶段

当前处于 **Golden Set 技术预览 P2 / 默认关闭安全融合**：代码和资产管线可上线，但不是默认开启的生产视觉。

后续按既定报告顺序继续：

1. Art Bible 人工确认、Teacher/Avatar 清稿与双人 IP Review。
2. M0 Golden Set 的 Teacher、Avatar、Core UI、飞行棋运行时纵切和人工决议。
3. 以通过的 Golden Set 批量覆盖六游戏，并让五子棋、俄罗斯方块旧风格向新风格迁移，持续保留回滚。
4. 第二桌面浏览器、Android、iPhone、Tablet、真实网络整形与 30 分钟 Synthetic Session。
5. 真实 Supabase 的 RLS/并发/备份回滚和多实例 Reward/AI 学习单写者改造。

本轮允许的发布范围仅为“双旗标缺失、默认关闭”的代码与素材，不得宣称人工 Golden Set 或 Production Ready。
