# 技术优化主线 T3 反馈与输入边界收口简报（2026-08-16 16:43）

## 一句话结论

状态：`LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`。T3 已在本地完成 FeedbackBus、GameplayInputGate 与 Tank 本地声像/震动 Adapter 的纵切、调用方接入、治理和全量回归；所有新能力默认关闭，三条共享 Gate 仍为 `BLOCKED`。

## 主线切换进度

| 方向 | 本地实现进度 | 最终闭环进度 |
| --- | ---: | ---: |
| 美术与品牌 | 41.7% | 38.2% |
| 界面与交互 | 75.0% | 67.8% |
| 游戏与局内体验 | 70.2% | 67.0% |
| 社交与玩家关系 | 59.1% | 59.4% |
| 经济成长与商业化 | 75.9% | 71.7% |
| 技术数据AI与跨平台 | 68.5% | 62.8% |
| 整体 | 65.5% | 61.4% |

权重：本地实现 `verified/implemented=100、partial=50、其余=0`；最终闭环 `verified=100、implemented=75、partial=40、planned=10、not_executed/blocked=0`。百分比是 242 项原子需求的治理快照，不是视觉验收或发布授权。

## 做了什么

- 新增默认关闭的 `FeedbackBus`：只接受固定语义 cue，并限制去重、速率、队列、监听器和生命周期；不保存原始输入、聊天或敏感信息。
- 新增默认关闭的 `GameplayInputGate`：Tetris 使用离散语义输入，Tank 使用完整九方向 `control_state + firing`，并绑定 match/revision/generation/sequence、TTL、乱序与过期拒绝。
- 新增 Tank 本地反馈 Adapter：只有真实用户手势解锁后才创建 AudioContext；只消费已接受 Authority 镜像的 fire/hit，支持左右声像、居中回退、八声部上限、震动独立开关与 dispose 清理。
- 接入 Tetris `tetris-rule-v3` 和 Tank `tank-authority-v1`；旧 Battle、Relay、本地同步与同步 AI fallback 全部保留。
- Terra Max 承担核心模块、专项测试和只读治理审计；主负责人否决了会丢失 Tank 斜向移动/按住开火的早期接口，补齐 sequence、过期、声部清理和证据防陈旧边界。
- 修正进度生成器、TECH-027、主线路由和状态对象：当前只认 `014E2886…07067` 的 T3 窄证据，T2/P4 完整矩阵只保留为 historical-as-of。

## 用户现在能看到什么

- 默认设置下没有新增强制声音、震动或输入延迟，现有六款游戏与 fallback 行为保持不变。
- 当前单一 Chromium 已确认 568×726 的 Home/Games/Playline/Profile、Tetris 与 Tank 可进入、可操作、可退出，零横向溢出和零裸 i18n key。
- T3 的技术反馈开关尚未作为默认玩家功能开启，因此用户暂时看不到完整跨设备声像/震动体验。

## 还没做什么

- `NOT_EXECUTED`：当前构建五档完整浏览器矩阵、第二桌面浏览器、物理 Android/iPhone/Tablet、真实网络整形和真机音频恢复。
- `NOT_EXECUTED`：真实 Supabase、多实例、人工清稿、Reviewer B、IP Review、Golden Set 与线上最新构建验证。
- T4–T7 尚未完成；下一批是 Xiangqi 优先、Gomoku 后续的 Worker AI、有界置换表与版本化开局库。

## 验证

- `npm run test:technical-optimization-t3`：`PASS`。
- `node qa/gameplay-upgrade.js`、`node qa/tank-controls.js`：`PASS`。
- `node qa/mainline-control-plane.js`、`node qa/progress-ledger.js`、`node qa/prove-current-build-evidence-contract.js`：`PASS`。
- `npm run quality:gates`：最终 `PASS`，21.1 秒；首次运行发现并修正 TECH-027 phase 枚举漂移后重跑通过。
- `npm test`：`PASS`，约 167.5 秒。
- `node scripts/build.js --check`：`PASS`；1,705,706 characters / 1,720,249 bytes / SHA-256 `014E2886711070F7B14CCCDF78E981C14871536BAE32A1A6369E823A56507067`。

## 风险与下一步

- 风险：本地单浏览器窄证据不证明跨浏览器、真机、网络、生产数据或真机 AudioContext 行为；默认关闭开关不能冒充已面向玩家发布。
- 下一步：进入 T4，建立不进入 Rule/Authority/Protocol 的 `BoardAIWorkerBroker`，先为 Xiangqi 冻结 Worker 取消、超时、候选合法性、有界置换表和版本化开局库，再复制到 Gomoku；主线程继续二次校验合法候选并保留同步 fallback。

## 发布状态

- `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`
- 未提交、未推送、未触发 GitHub Pages 或 Render；线上仍保持既有发布基线。

## 追溯入口

- active task：`requirements/active/technical-optimization-mainline-p0-20260816/`
- 需求 ID：`GAME-014`、`GAME-037`、`GAME-038`、`GAME-044`、`TECH-027`、`TECH-033`、`TECH-044`
- 当前浏览器证据：`requirements/active/latest-browser-visible-matrix-prove-p4-20260815/evidence/current-build-single-browser-verification-t3-202608161627.json`
- 核心实现：`public/src/core/15-feedback-bus.js`、`public/src/core/16-gameplay-input-gate.js`、`public/src/core/17-local-feedback-adapter.js`
- 专项 QA：`qa/feedback-bus.js`、`qa/gameplay-input-gate.js`、`qa/local-feedback-adapter.js`
- 详细进度：`简易报告/项目总需求进度报告-20260816.md`、`简易报告/技术数据AI与跨平台进度报告-20260816.md`
