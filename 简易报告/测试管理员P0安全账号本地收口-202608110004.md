# 测试管理员 P0 安全账号本地收口

时间：2026-08-11 00:04（Asia/Tokyo）

## 做了什么

- 新增只由 Render/本机服务端环境变量精确绑定的测试管理员；启动配置不完整时服务端直接拒绝启动。
- 私有档案显示 `∞ G Coins`、`Lv.MAX`、测试管理员徽章，并虚拟拥有当前商城目录；不把特殊字段或凭证保存到浏览器本地存储。
- 测试房间、赛事控制面、公开档案、排行榜、Presence、Lobby、好友、Block、举报和玩家私聊均与正式玩家隔离。
- 测试局不写正式奖励、经济流水、Replay、AI 学习、Analytics 或 Supabase outbox。
- 未来新增权限、商品和系统能力不会自动继承，必须显式加入白名单并新增回归。

## 主负责人复核修正

- 修正好友请求兼容 payload、测试沙盒能力名、外部赛事 participants 广播和安全测试的 sender-excluded move 语义。
- Render 秘密变量先写 UID/用户名/密码，最后写启用开关，避免半配置重启。
- 修复离房后旧 `room_update` 晚到会复活旧房间、阻塞下一次建房的生命周期竞态，并增加固定回归。
- 发布前 Terra Max 审计发现并由主负责人修正：Render 与运行时 UID 规则不一致、沙盒房间设置可翻转、结算后仍可发送局内聊天/表达三项风险。
- 沙盒现在始终私有且禁止观战；结算会清空局内社交临时态、取消观众延迟投递并拒绝后续发送/同步；Supabase 启用时管理员远端引导失败会在监听前 fail-closed。

## 测试

- `qa/test-admin-contract.js`、`qa/test-admin-ui-contract.js`、`qa/test-admin-online.js`、`qa/test-admin-security.js` 全部通过。
- `qa/tournament.js`、Security、Supabase Adapter、Reconnect、E2E 等关联回归通过。
- 修复后连续三轮完整 E2E 均为 `E2E_ALL_PASS`。
- 完整 `npm test` 167.7 秒通过。
- 双构建物理文件均为 996,728 字节，SHA-256 一致：`e8b8d37c66d8843b61f040eaf5028995a5ebf5e30fdd6abff6036ab84ede304e`。

## 边界与回滚

- 实际管理员密码和 UID 不进入仓库、报告、日志或截图。
- 将 `TEST_ADMIN_ENABLED=0` 并重启即可关闭所有特殊能力；由于测试账号不写正式经济/社交/对局数据，不需要数据回滚。
- 2026-08-11 00:57（Asia/Tokyo）Render 部署 `dep-d9sv99f40ujc73dvlru0` 已上线提交 `da3d05c`；GitHub Pages 与 Render 首页均为 996,728 字节，SHA-256 与本地构建逐字一致。
- 线上浏览器确认测试管理员私有档案显示测试徽章、`Lv.MAX` 与 `∞ G Coins`，控制台无 warning/error。
- 线上临时访客 WebSocket 确认管理员公开档案不可读、沙盒不进 Lobby 且不能加入/观战；沙盒设置不可翻转，测试局结算为零奖励且管理员正式战绩不变。访客随后注销删除。
