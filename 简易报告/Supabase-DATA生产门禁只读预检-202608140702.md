# Supabase DATA 生产门禁只读预检任务收口简报（2026-08-14 07:02）

## 一句话结论

状态：`BLOCKED_EXTERNAL_WITH_LOCAL_PREFLIGHT_COMPLETE`。本机 DATA 工具链与恢复隔离安全边界已收紧，但没有真实凭证，因此生产 Gate 继续阻塞。

## 做了什么

- 只记录 Supabase/API/DB 环境变量“存在或不存在”，没有读取或输出值。
- 验证 `psql / pg_dump / pg_restore` 已可用，Production Ops 默认 dry-run 为零连接、零写入、零备份。
- 用红—绿回归修复同一 Supabase 项目、不同数据库名仍可能执行 `pg_restore --clean` 的缺陷。
- 运行 Schema、Production Readiness 和 fake Adapter 专项，并将证据归类为 `LOCAL_STATIC_OR_FAKE_ONLY`。

## 用户现在能看到什么

- 本批没有玩家可见 UI 变化；变化只在生产数据运维的安全防线和回归合同。

## 还没做什么

- `NOT_EXECUTED`：真实备份、迁移、RLS、原子 RPC、并发、隔离恢复、非破坏回滚、双实例和 Cluster。
- `BLOCKED`：当前进程没有真实项目 URL、server-only service role、数据库直连、隔离项目和可验证加密备份位置。

## 验证

- Production Ops 默认 dry-run：`PASS`。
- 同项目异库拒绝 / 不同项目显式确认：`PASS`，使用纯假 URI，未联网。
- `node qa/production-readiness-contract.js`：`PASS`。
- `node qa/supabase-schema.js`：`PASS`。
- `node --experimental-websocket qa/supabase-adapter.js`：`PASS`，26 assertions。
- 完整 `npm test`：`PASS`，141.4 秒。
- 确定性双构建：`PASS`，两次均为 1,362,068 characters / 1,376,602 bytes / SHA-256 `BFBD2109B77B1F4C5E070DAB6B98C806E25AD621ACBE3E2E83AD5C5FB7130CAE`。

## 风险与下一步

- 风险：静态 SQL、fake Adapter、dry-run 和本地测试都不能证明真实 RLS、数据恢复或多实例一致性。
- 下一步：只有获得受控真实环境后，才按“加密备份 → 事务迁移/验收 → 并发 → 不同项目恢复 → 非破坏回滚 → 双实例”的顺序执行；在此之前继续准备 ART Gate，不开启 Cluster。

## 发布状态

- `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`
- 未提交、未推送、未触发 GitHub Pages 或 Render。

## 追溯入口

- active task：`requirements/active/supabase-production-data-preflight-p0-20260814/`
- 需求 ID：`GAME-009 / SOC-027 / SOC-028 / ECO-006 / TECH-016 / TECH-017 / TECH-018 / TECH-020 / TECH-021 / TECH-022`
- 详细证据：`requirements/active/supabase-production-data-preflight-p0-20260814/evidence/local-data-preflight-202608140702.json`
