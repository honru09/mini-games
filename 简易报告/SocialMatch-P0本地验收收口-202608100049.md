# Social Match P0 本地验收收口

时间：2026-08-10 00:49（Asia/Tokyo）

状态：`LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`

## 已完成

- 新增 `match-expression-v1`：十个稳定 Emoji 语义 ID、六个稳定快捷语 ID、目标席位和三类成对消息。
- 服务端权威签发发送者 UID、玩家席位、时间和协议；拒绝伪造字段、错误 match、非法表达/目标。
- 实现账号 × 对局 `eventId` 幂等、10 秒/60 秒/单局频控、双向 Block 与逐接收者过滤。
- 观众只读，访客、AI、非当前对局玩家不可发送；表达不写入 moveLog、Replay、奖励、AI 学习、Analytics 或数据库。
- Seat 展示公开 Avatar、Frame、Effect、NameFx、Lang；真人头像/名字可打开公开 Profile，未暴露 owned、余额、token 等私有字段。
- Command Slot 增加表达盘、目标选择与本地静音；每个 Seat 最多三条气泡，退出、重开和销毁时清理。
- reduced-motion 使用静态降级；三语言词典保持同构。

## 主负责人审核与修正

- 修复玩家 Seat 的嵌套按钮结构，拆为独立头像按钮和名字按钮。
- 修复本人通过服务端 `profile_data` 打开公开档案时错误显示“加好友”的状态。
- 修复测试桩缺少 `addEventListener` 时的兼容问题。
- 完整测试首次发现三语言缺少 `profile_title`，已补齐 zh-CN/en-US/uk-UA 后重跑全链通过。
- Terra Max 初次终审受到服务端 429/不可读返回影响；重试后完成客户端与服务端并行审计。主负责人复核并修正其有效发现：Seat 语言旗帜、目标头像气泡、`senderUid × eventId` 客户端键、900ms 视觉冷却、事件级举报、观战离席确认/清理、44px 触控、本地静音立即清理，以及延迟观战计时器/旧 match 阻断。

## 测试与浏览器证据

- `node qa/social-match-contract.js`：ALL_PASS。
- `node --experimental-websocket qa/social-match-online.js`：ALL_PASS。
- `npm test`：ALL_PASS，完整主链 144.1 秒。
- `npm run quality:gates`：`QUALITY_GATES_FAST_ALL_PASS`。
- `npm run test:progress-ledger`：`PROGRESS_LEDGER_ALL_PASS`。
- `git diff --check`：通过。
- 双次构建 SHA-256：`030CFDFE60A6ED8345EE6B326B564AE192D0C852DE1C6AA065B47200D14F2394`。
- 本地 in-app 浏览器对局态复核 1440×900、1024×768、390×844、844×390：固定全视口、页面无滚动、表达盘与气泡可用。

## 明确未完成

- `ART-024 / ART-025 / SOC-017`：十枚原创 Honru Emoji、人工清稿、Reviewer B、IP Review、Golden Set 与最终投掷动画素材。
- `GAME-025 / SOC-019`：自由文本房间聊天；本轮只有白名单快捷语。
- 全站 PlayerIdentity 统一仍需覆盖聊天、排行榜、个人页和更多玩家入口。
- 真实 Supabase、第二浏览器、Android/iPhone/Tablet 真机、真实网络整形仍未执行。
- 未 commit、未 push、未触发 GitHub Pages 或 Render 部署。

下一主线：`UI Repair P0`，独立审计房间、商城、聊天、Profile、Avatar/Frame 和响应式排版。
