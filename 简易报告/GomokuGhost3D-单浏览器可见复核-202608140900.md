# Gomoku Ghost3D 单浏览器可见复核（2026-08-14 09:00）

## 一句话结论

状态：`LOCAL_ACCEPTED_SINGLE_BROWSER_PARTIAL`。五子棋的默认关闭 Three 预览已在一个本地 Chromium 实际打开、落子、AI 回合和 reduced-motion 下复核；它仍只是单浏览器部分证据，不能当作真机、性能或正式美术通过。

## 做了什么

- 用默认设置进入五子棋，确认 Ghost3D 不出现、Wave B Canvas 仍是可聚焦的 15×15 棋盘，键盘和触控回退保留。
- 临时开启本地 `mg_ghost3d_gomoku_v1` 并选用 `BALANCED`，确认 Three 程序化立体棋盘首帧 ready、Raycast 落子和 AI 回合均正常。
- 通过 CDP 打开 reduced-motion，再次确认三维层与旧 Canvas/触控路径可操作且保持静态反馈。
- 结束后删除两个本地 Ghost3D flag，并清除 reduced-motion 模拟；默认产品状态没有被改开。

## 用户现在能看到什么

- 开发预览开启时，五子棋会显示带厚度、灯光、透视和实体棋子的程序化 3D 棋盘；关闭预览仍回到现有稳定的棋盘。
- 正常默认使用者暂时看不到这项预览，因为它保持默认关闭。

## 还没做什么

- `NOT_EXECUTED`：第二桌面浏览器、Android、iPhone、Tablet、真实网络整形和真实帧率/GPU/温度测试。
- `NOT_EXECUTED`：GLB/纹理、正式美术接入、Reviewer B、IP Review 和用户 Golden Set。
- `NOT_EXECUTED`：提交、推送、GitHub Pages 或 Render 发布。

## 验证

- 单一 Codex in-app Chromium：默认关闭回退、BALANCED 首帧 ready、Raycast 落子、AI 回合、CDP reduced-motion、console warn/error=0：`PASS`。
- `npm test`（153.6 秒）与双构建 `BFBD2109…30CAE`：`PASS`。
- 可见复核结果已写入可版本化证据 JSON；会话中的临时截图不作为长期仓库证据引用。

## 风险与下一步

- 风险：单一 Chromium 和临时本地 flag 不能证明跨浏览器、真机、网络、性能或美术审批。
- 下一步：保留 `TECH-049=partial`，等待设备/网络 Gate 的真实环境；同时继续 CLOSE 范围内不跨越美术审批的收口工作。

## 发布状态

- `LOCAL_ACCEPTED_SINGLE_BROWSER_PARTIAL`
- 未提交、未推送、未触发 GitHub Pages 或 Render。

## 追溯入口

- active task：`requirements/active/gomoku-ghost3d-vertical-slice-p0-20260812/`
- 需求 ID：`TECH-049`
- 详细证据：`requirements/active/gomoku-ghost3d-vertical-slice-p0-20260812/evidence/single-browser-visible-verification-202608140900.json`
