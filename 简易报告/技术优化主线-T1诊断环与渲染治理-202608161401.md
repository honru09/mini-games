# 技术优化主线 T1 诊断环与渲染治理简报（202608161401）

## 一句话结论

T1 本地技术基线已完成并纳入构建与自动门禁；产品运行时默认不启用，未改变游戏、协议、奖励、数据或线上版本。

## 本次完成

- `ClientDiagnosticsRing`：64 条有界内存环、5 分钟 TTL、三类事件和固定分类字段、ID 类别化哈希；敏感字段 fail-closed；无网络、localStorage、监听器、定时器或自动外发。
- `RendererRuntimeGovernor`：保留 HIGH/BALANCED/LOW 静态 DPR 上限；Dynamic DPR 默认关闭，仅输出带 hysteresis/cooldown 的建议；resize 合帧、单循环所有权、hidden/suspend/reduced-motion/context generation/dispose 与数值诊断。
- 两个模块已加入 `scripts/build.js`，但尚未实例化到六款 Renderer；保留现有 DOM/2D 与 Ghost3D fallback。
- 新增并接入 `npm run test:technical-optimization-t1`、Quality Gates 与完整 `npm test` 的 posttest。

## 审核与修正

- Terra Max 审核确认 Foundation 外部 Interface 未扩大，Renderer 不接触规则/Authority/协议/持久化。
- 主代理补齐 `pin/prompt/url/uri/body/text/content` 等敏感键拒绝覆盖，并增加构建图/专项测试合同。
- 未发现需要返工的代码差异。

## 验证

- `node --check public/src/core/13-client-diagnostics-ring.js`：通过。
- `node --check public/src/core/13-renderer-runtime-governor.js`：通过。
- `npm run test:technical-optimization-t1`：通过（11 项诊断断言、39 项渲染治理断言）。
- `node qa/ghost3d-foundation.js`：通过。
- `npm run quality:gates`：最终退出码 0。
- 完整 `npm test`：最终退出码 0；此前的报告身份漂移已修正并重新通过全链。
- `node scripts/build.js`：T1 当时构建记录保留为历史；当前 T2 构建为 1,647,354 characters / 1,661,897 bytes / SHA-256 `1B26D7D58DCC6B4A9BE55BE09C0D787E1B1AF08CBD6C54D4175BF73064F2D1DF`。
- 确定性双构建：字符数、字节数与 SHA-256 完全一致。
- 当前单一 Codex in-app Chromium 的完整 T2 复核已完成：五档 viewport × 四区 20/20、六款本地 AI Game Stage、三语言、双主题、reduced-motion、forced-colors、零横向溢出、零裸 key、console warn/error=0；证据见 `requirements/active/latest-browser-visible-matrix-prove-p4-20260815/evidence/current-build-single-browser-verification-t2-202608161530.json`。T1 文件继续作为兼容的当前构建哈希绑定记录保留。

## 未完成与风险

- Tetris/Tank 等六款 Renderer 尚未接入 Governor；没有真实 WebGL、帧耗、GPU/内存、热量、第二浏览器、真机或真实网络证据。
- 诊断环仍默认关闭且不自动采集；真实外发、生产 Supabase、多实例和三条共享 Gate 仍保持 `BLOCKED/PARTIAL`。
- 旧 P4 结构化浏览器矩阵对新构建无效；当前已有 T1 窄范围证据，完整 P4 深矩阵仍需重新采集。

## 当前与下一步

- 当前阶段：`CLOSE`；T1 为 `implemented_local`，总体仍 `LOCAL_ONLY / NOT_RELEASED`。
- 下一主线：T2 已完成；后续进入 T3（FeedbackBus 生命周期与 GameplayInputGate），继续保持旧内联/同步 fallback。
- 未收到明确“输出线上/推送/部署”指令前，不执行发布操作。
