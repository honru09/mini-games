# 技术优化主线 T0 授权与缩放修复简报（202608161331）

## 一句话结论

技术优化与机器可完成 Gate 自动推进已正式纳入主线；T0 合同、去重归类、缩放缺陷修复和回归已完成，后续技术 Wave 仍是本地 planned，未发布。

## 做了什么

- 将 Tank Delta/Prediction、Renderer 生命周期/DPR/Context Loss、Haptic/Input Buffer/空间音频、Worker AI、APM/Action Entropy、脱敏诊断、按需加载/SW 预热、Server 深模块与测试隔离归回既有 Requirement，没有创建重复原子 ID。
- 固化 `requirements/active/technical-optimization-mainline-p0-20260816/` 的需求、深模块合同、Wave 计划和验收边界。
- 机器可验证子 Gate 改为默认自动推进；复合 Gate 仍必须等待外部环境或人工证据。
- 移除 viewport 的 `maximum-scale=1,user-scalable=no`，保留沉浸式对局局部滚动锁，并加入专项回归。
- 将用户确认的两张 M0 图写入 Art Bible、Design System、ImageGen Prompt、M0 执行状态和 Art Gate 证据链。

## 用户现在能看到什么

- 浏览器可正常缩放；游戏内页面滚动隔离保持不变。
- 现有平台、规则、奖励、账号、社交和未审批美术运行时行为不变。
- 两张 M0 图现在是后续原创 UI/角色/局内视觉的唯一 North Star，但候选仍默认关闭。

## 还没做什么

- Tank Delta codec、prediction reconciliation、Renderer Governor、Dynamic DPR、Worker AI、输入缓冲、空间音频、诊断环、按需游戏 chunk、Server Boundary 拆分尚未进入 T1–T7 实现。
- 当前缩放修复构建尚未重新采集完整浏览器可见矩阵；旧 P4 已明确标记 historical-as-of。
- 第二浏览器、物理 Android/iPhone/Tablet、真实网络、真实 Supabase、多实例、人工清稿、Reviewer B、IP/法律、Golden Set 仍未执行。

## 验证

- `npm run quality:gates`：通过（约 17.2 秒）。
- `npm test`：通过（约 143 秒，含 E2E 与 posttest）。
- `npm run test:i18n`：通过（约 1.1 秒）。
- `node qa/dom-smoke.js`：通过（约 9.8 秒）。
- `node qa/prove-current-build-evidence-contract.js`：historical P4 防陈旧通过。
- 两次 `node scripts/build.js`：字节/字符数/哈希完全一致。
- 当前构建：1,597,513 characters / 1,612,056 bytes / SHA-256 `ED91C148936E13EE4BCF9BB2A81FDAC9AFA5DFCE9F94F03F89210B21FFB47C90`。

## 风险与下一步

下一步先执行 T1 的 ClientDiagnosticsRing 本地纵切与 RendererRuntimeGovernor 基线，再进入 Input/Feedback、Worker AI、Tank wire codec；每个 Wave 完成后重新生成分类报告和三日志。旧 P4 证据必须在新浏览器矩阵完成前保持历史状态。

## 发布状态

`LOCAL_ONLY / NOT_RELEASED`。未 commit、push、GitHub Pages、Render 或 Supabase 写入。

## 追溯入口

- `requirements/active/technical-optimization-mainline-p0-20260816/`
- `requirements/PRODUCT_REQUIREMENTS_LEDGER.json`
- `requirements/MAINLINE_CONTROL_ROUTING.json`
- `PROJECT_STATUS.json`
- `qa/technical-optimization-mainline-contract.js`
- `qa/approval-gate-policy.js`
