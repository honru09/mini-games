# Tank Ghost3D P5 本地纵切收口（202608150945）

## 一句话结论

坦克大战已完成第六款 Ghost3D 的本地 default-off 纵切、自动化回归、Terra Max 终审和单一 Chromium 部分可见复核；三条外部门禁仍阻塞，当前没有上线。

## 主线切换进度

权威路由仍为 `CLOSE`，本批本地收口证据已准备好，下一授权阶段为 `PROVE`。以下是 242 项唯一需求的治理快照：

| 方向 | 本地实现进度 | 最终闭环进度 |
| --- | ---: | ---: |
| 美术与品牌 | 40.3% | 37.1% |
| 界面与交互 | 75.0% | 67.8% |
| 游戏与局内体验 | 69.2% | 66.4% |
| 社交与玩家关系 | 59.1% | 59.4% |
| 经济成长与商业化 | 75.9% | 71.7% |
| 技术数据AI与跨平台 | 66.7% | 61.7% |
| **整体** | **64.7%** | **60.9%** |

权重：本地实现 `verified/implemented=100、partial=50、其余=0`；最终闭环 `verified=100、implemented=75、partial=40、planned=10、not_executed/blocked=0`。这些数字保留真实浏览器、真机、Supabase、人工美术和发布 Gate 缺口，不等于视觉验收或上线授权。

## 做了什么

- 新增 Tank Ghost3D Presenter、Three r185 程序化 Arena、bridge、样式与五类专项 QA。
- 精确使用 Wave B + `mg_ghost3d_tank_v1 === '1'` 双 gate；任何缺失、异常或失败都回到原有 DOM Tank。
- 只消费已提交 local/AI 或 accepted `tank-authority-v1` raw receipt；保留 DOM 棋盘、摇杆、D-pad、键盘、独立开火、HUD、Wave C、社交和结算所有权。
- 固定 5 tanks / 128 projectiles / 221 terrain 上限；补齐 tick-0、重复/冲突 tick、畸形 tick、gap 静态收敛、generation/adapter epoch 和 import retry 防火墙。
- Terra Max Standards/Spec 复核后修正旧 revision FX timeline、同 tick/NaN receipt 和动态 import 失败永久锁死。

## 用户现在能看到什么

默认访问仍是可玩的 DOM 坦克局内界面，不会自动下载或覆盖 3D。临时本地 opt-in 时，当前 Arena 会显示只读的程序化立体层；Canvas 不抢输入，DOM 开火、摇杆、方向键、键盘和 HUD 继续工作。单一 Chromium 已确认桌面、平板、手机竖屏和横屏无横向溢出，D-pad 为 44px。

## 还没做什么

- `NOT_EXECUTED`：独立第二桌面浏览器、Android、iPhone Safari、Tablet、物理触控、真实延迟/抖动/丢包/乱序、低端 FPS/GPU/发热/内存。
- `NOT_EXECUTED`：最终修正后的 visible reduced-motion 重模拟、5 人真实可见联机。
- `BLOCKED`：Reviewer B、IP Review、用户 Golden Set、ART-035 正式 Tank runtime。
- 本批没有 Supabase、Cluster、Rule、Authority、Protocol、Reward、Replay、AI、经济或数据库施工。

## 验证

- `npm run test:tank-ghost3d`：ESM 32、Renderer 65、Presenter/bridge 49、layout 18、cache 15，全部通过。
- Ghost3D Foundation、Tank controls/authority、Tank/Xiangqi density、Game Stage input continuity、Immersive Shell、i18n、DOM smoke、reconnect/spectator/E2E/load/memory/timer 回归通过。
- `npm run quality:gates`：通过。
- 完整 `npm test`：通过，约 144.6 秒。
- 确定性双构建一致：1,579,631 characters / 1,594,165 bytes / SHA-256 `41E7C7562B9289ABA27D237D9C806D1B9565B71293453EDC47F9BF05AC5F383E`。
- 单一 Codex in-app Chromium：default-off、临时 HIGH、1440×900/1024×768/390×844/844×390、Canvas pointer-through、DOM 开火、零横溢出、console warn/error=0。

## 风险与下一步

`GAME-051` 与 `GAME-044` 保持 `implemented`，`TECH-049` 保持 `partial`。下一步不是继续堆新 Renderer，而是按总指挥进入 `PROVE / GATE-DEVICE-BROWSER-NETWORK`；第二浏览器、真机和真实网络证据到位前，不能宣称跨设备视觉验证或生产就绪。美术仍遵守 `GATE-ART-GOLDEN-SET`，未审批 Tank 素材不进 runtime。

## 发布状态

`LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`。本批未 commit、未 push、未触发 GitHub Pages、未触发 Render、未修改线上配置或线上数据。

## 追溯入口

- Active task：`requirements/active/tank-ghost3d-vertical-slice-p5-20260815/`
- 需求：`GAME-051`、`TECH-049`、维护 `GAME-044`
- 浏览器证据：`requirements/active/tank-ghost3d-vertical-slice-p5-20260815/evidence/single-browser-visible-verification-202608150945.json`
- 实现：`public/src/games/tank.js`、`public/src/games/tank-ghost3d-presenter.js`、`public/three/tank-entry.js`
- 专项：`qa/tank-ghost3d-contract.js`、`qa/tank-ghost3d-renderer.js`、`qa/tank-ghost3d-esm-graph.js`、`qa/tank-ghost3d-layout.js`、`qa/tank-ghost3d-cache.js`
