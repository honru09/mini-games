# TECH-033 性能预算治理简报（2026-08-13 23:30）

## 一句话结论

TECH-033 已完成本地预算合同收口：首屏、游戏舞台、动效、素材和长列表都有加载、清理、fallback 与证据边界。真实设备 FPS、GPU/纹理内存和网络体验仍未执行，线上未变。

## 做了什么

- 按 `gsap-performance` skill 固定 transform/opacity 优先、批量读写、有限 tween、离屏暂停和 dispose/kill；禁止用 width/height/top/left 等布局属性做运动。
- 新增 `requirements/PERFORMANCE_BUDGET_CONTRACT.md`，覆盖 Shell、Game Stage、Motion、Assets、Lists 五层预算。
- 新增 `qa/performance-budget-contract.js` / `npm run test:performance-budget`：检查 Manifest integrated ID、路径安全、动效布局属性、reduced-motion/清理和敏感信息边界。
- TECH-033 更新为 `implemented`；真实设备和网络证据仍保留为外部门禁。

## 用户现在能看到什么

- 这次没有新增网页或游戏功能；后续动效、素材和游戏包必须遵守同一预算，不会为了“更多动效”牺牲输入和清晰度。
- 当前运行时没有把未审批 source-only 图片强行放入 integrated/default-on 资产。

## 还没做什么

- `NOT_EXECUTED`：Android/iPhone/Tablet、第二浏览器、真实网络整形、冷/热加载、FCP/LCP、低端 FPS、GPU/纹理内存和真实 dispose 测量。
- `BLOCKED`：人工清稿、Reviewer B、IP Review、Golden Set 与真实生产数据 Gate。
- 本批不修改规则、协议、奖励、数据库或线上配置。

## 验证

- `npm run test:performance-budget`：PASS（Manifest 17 项；integrated ID 唯一）。
- `npm run reports:progress` / `node qa/progress-ledger.js` / `node qa/mainline-control-plane.js`：PASS。
- `npm run quality:gates`：PASS（含性能、Bug Intake、ADR、简报和纵切合同 Gate）。
- 完整 `npm test`：PASS（137.3 秒）。
- `git diff --check`：PASS。
- `node scripts/build.js`：PASS；`public/index.html` 1,353,257 字符 / 1,367,874 字节，SHA-256 `2E466A3B59CEC8B7B1323DC6FD61375395E2497BE8C837C0D01A789D02731E93`。

## 风险与下一步

- 风险：静态预算不能证明真实 60fps 或内存安全；低端设备和网络整形必须保留原始测量。
- 下一步：继续 CLOSE 的具体表现纵切；进入 PROVE 后按本合同记录真实冷/热加载、FPS、GPU/纹理内存和网络数据。

## 发布状态

- `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`
- 未提交、未推送、未触发 GitHub Pages 或 Render；线上保持 `bd49e6d` / `da3d05c` 基线。

## 追溯入口

- 需求：`TECH-033`。
- 合同：`requirements/PERFORMANCE_BUDGET_CONTRACT.md`。
- QA：`qa/performance-budget-contract.js`、`npm run test:performance-budget`。
- active task：`requirements/active/performance-budget-p1-20260813/`。
- 使用 skill：`C:\Users\wangxr\.codex\skills\gsap-performance\SKILL.md`。
