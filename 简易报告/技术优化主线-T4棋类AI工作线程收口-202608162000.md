# 技术优化主线 T4 棋类 AI 工作线程收口

时间：2026-08-16 20:00（Asia/Tokyo）  
状态：`LOCAL_IMPLEMENTED / DEFAULT_OFF / NOT_RELEASED`

## 完成内容

- 新增 `BoardAIKernel`、`BoardAIWorkerBroker` 与版本化 Worker wire，隐藏 Xiangqi/Gomoku 搜索、Zobrist、4096 项 LRU 置换表、开局库、预算、超时和取消复杂度。
- 象棋与五子棋只在本地 AI、非观众、非联机且显式打开 `boardAIWorkerV1` 时启用；Worker 只返回候选 ID/评分，主线程再次验证合法性。
- 五子棋合法候选超过 200 时保留同步路径，不截断候选；同步搜索、DeepSeek 与个人学习继续作为 fallback。
- reset、restore、换局、spectator、超时、崩溃和 destroy 均清理请求、Worker 与 TT；不改变 Rule、Authority、Reward、Replay 或网络协议。

## 验证

- `npm run test:technical-optimization-t4` 通过，覆盖 Kernel、Worker VM/wire、Broker 生命周期、象棋/五子棋调用方 parity 与 fallback。
- T4 历史构建：1,785,103 characters / 1,799,646 bytes / SHA-256 `3C714ECACE20B4CAC4020B53AAFB2F5F9F68F68884D18F71BD3C826BDE3811EE`；后续 T5 构建产生后只作为 historical-as-of。

## 剩余

- 真实低端设备 Worker 性能、第二浏览器、物理真机与生产遥测仍未执行。
- 当前不发布；T4 完成后主线已进入 T5。
