# Social Match 会话能力复核修正

时间：2026-08-10 06:20（Asia/Tokyo）  
范围：局内表达 `match-expression-v1` 的连接生命周期修正

## 结论

该问题已经落地修正。`public/src/online/03-websocket.js` 的 `online.resetState()` 不再在同一 WebSocket 连接内清空已协商 `capabilities`；只有 `connected === false` 时才清空。这样会话失效触发本地注销、退出房间、重开或对局重置后，重新认证仍可恢复局内表达入口；真实断开仍会清空能力，避免跨连接残留。

## 验证

- `node qa/social-match-contract.js`：`SOCIAL_MATCH_CONTRACT_ALL_PASS`
- `node --experimental-websocket qa/social-match-online.js`：`SOCIAL_MATCH_ONLINE_ALL_PASS`
- 双次 `node scripts/build.js` 产物 SHA-256：均为 `A0F8D8139D8884F4F6D394F308778B249321D0D6E762891DC90FDEF04709A95B`
- 此前完整 `npm test` 已通过；本次没有改动游戏规则、服务端协议或生产数据。

## 外部边界

尝试重新打开 `http://127.0.0.1:8080` 做浏览器复核时，当前 in-app Browser 被用户保存权限阻断。根据浏览器安全规则没有绕过、换浏览器或使用间接通道；该项保留为外部浏览器验收闸门，不能伪称已完成。

本批未 commit、未 push、未部署。
