# TECH-039 ADR 架构决策治理简报（2026-08-13 22:30）

## 一句话结论

TECH-039 已完成本地治理收口：今后涉及协议、数据、渲染、支付、跨端、安全、备份/回滚或发布边界的重大选择，都必须先留下一份可审阅的 ADR。此批没有替用户作具体生产架构决定，也没有上线。

## 做了什么

- 新增 `requirements/ADR/README.md`：规定 ADR 触发条件、编号、`proposed/accepted/superseded/rejected` 状态和与需求台账的关系。
- 新增 `requirements/ADR/000-template.md`：固定背景、决策、范围、替代方案、证据、风险、兼容和回滚字段。
- 新增 `qa/adr-contract.js` / `npm run test:adr`：检查模板、状态、`NOT_EXECUTED/BLOCKED` 诚实记录、TECH-039 追溯和敏感信息不泄漏。
- TECH-039 更新为 `implemented`；具体协议、Supabase、支付和跨端选择仍等待各自独立 active task 与真实证据。

## 用户现在能看到什么

- 用户暂时看不到新的页面或功能；这是防止重大技术选择丢失上下文的项目治理能力。
- 后续如果提出“换数据库/渲染器/支付/跨端”等大改动，会先有一份短 ADR 解释取舍和回滚，不会悄悄改变主线。

## 还没做什么

- `NOT_EXECUTED`：尚未为具体协议、真实 Supabase、支付或跨端方案创建新的 `accepted` 生产 ADR。
- `BLOCKED`：真实 Supabase、设备/网络和人工美术 Gate 仍按主线路由保持阻塞。
- 本批没有修改 Rule/Authority/Protocol/Reward/Replay、数据库、运行时素材或线上配置。

## 验证

- `npm run test:adr`：PASS。
- `npm run reports:progress` / `node qa/progress-ledger.js` / `node qa/mainline-control-plane.js`：PASS（242 项、7 份报告、146/32/48/16 路由）。
- `npm run quality:gates`：PASS（含 ADR、简报和纵切合同 Gate）。
- 完整 `npm test`：PASS（168.6 秒）。
- `node scripts/build.js`：PASS；`public/index.html` 1,353,257 字符 / 1,367,874 字节，SHA-256 `2E466A3B59CEC8B7B1323DC6FD61375395E2497BE8C837C0D01A789D02731E93`。
- `git diff --check`：PASS。

## 风险与下一步

- 风险：ADR 只能记录决策，不能替代真实 Supabase、设备、网络、人工审批或发布验收；`accepted` 不等于 `verified` 或生产就绪。
- 下一步：继续 CLOSE 表现层；有真实架构选择时先创建对应编号 ADR，再按主线 Gate 实施。

## 发布状态

- `LOCAL_ACCEPTED_AWAITING_RELEASE_COMMAND`
- 未提交、未推送、未触发 GitHub Pages 或 Render；线上保持 `bd49e6d` / `da3d05c` 基线。

## 追溯入口

- 需求：`TECH-039`。
- ADR 入口：`requirements/ADR/README.md`、`requirements/ADR/000-template.md`。
- QA：`qa/adr-contract.js`、`npm run test:adr`。
- active task：`requirements/active/adr-governance-p1-20260813/`。
