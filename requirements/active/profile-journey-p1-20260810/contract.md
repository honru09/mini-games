# Profile Journey P1 冻结合同

1. Journey 是只读表现投影，不产生服务端 mutation，不修改胜场、成就或 owned。
2. 最近称号只从服务端 `mastery` 或服务端 `wins` 的共用派生结果选择；相同档案必须得到相同目标。
3. 成就只显示本人已解锁数量与当前总目录；收藏只显示分类去重计数，不暴露购买记录、价格或他人 owned。
4. 三张卡分别复用 Games 焦点、既有成就弹层和商城；不得增加协议或数据库字段。
5. 系统文案必须三语同构、移动端可换行、按钮至少 44px；不使用强迫、羞辱或虚假稀有度文案。
