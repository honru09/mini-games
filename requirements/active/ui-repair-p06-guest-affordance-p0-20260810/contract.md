# UI Repair P0.6 冻结合同

1. 访客可打开商城并查看商品/身份组合预览。
2. 购买控件显示 `aria-disabled` 与三语言限制提示，直接调用 `requestPurchase` 不发送 WebSocket。
3. 好友请求/处理、移除、Block/Unblock、Report 与主动邀请在 UI 和客户端方法两层阻断。
4. 访客仍可进入房间、接受/拒绝收到的房间邀请并进行允许的临时游戏行为。
5. 正式账号行为不变；服务端持久化授权保持最终权威。
