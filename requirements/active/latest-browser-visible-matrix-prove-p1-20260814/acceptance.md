# Latest Browser Visible Matrix PROVE P1

> **Historical policy note（historical-as-of，2026-08-16）：** 下文关于设备、第二浏览器、真实网络或 Supabase 的 `BLOCKED` 表述仅是本批形成时的历史验收快照。当前设备/浏览器/网络与 Supabase Gate 均为 `NON_BLOCKING_FOR_DEVELOPMENT`；缺失的真实环境证据保持 `NOT_EXECUTED / RELEASE_EVIDENCE_PENDING`。这不表示外部证据已经完成，也不授予跨设备、生产就绪或发布结论；下方旧结果仅为历史留档，不再阻塞开发，发布仍须当前用户明确命令。

- 只证明当前最新本地构建在一个 Codex in-app Chromium 会话中的可见/交互证据。
- 必须记录真实 viewport、页面可见、横向溢出、44px 操作、主题、语言、reduced-motion、控制台和返回/关闭生命周期。
- 旧截图、DOM/VM、响应式 CSS 静态合同不能替代本批浏览器证据。
- 第二浏览器、Android、iPhone、Tablet 物理设备、真实网络整形、真实低端 FPS/发热/音频/锁屏恢复均保持 `NOT_EXECUTED/BLOCKED`。
- 不修改 Rule、Authority、Protocol、Reward、Replay、Supabase、经济、美术 runtime 或线上配置。
