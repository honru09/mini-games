# Shared Change Request

## `public/src/ui/07-roster.js`

请求由 Master 最小修改 `avatarStageNode()`：允许在突出预览中显式传入 `animate:true`，默认列表保持 poster 以控制性能；不得改变 Avatar ID、商品或 Profile 数据。

## `public/index.html`

仅通过 `node scripts/build.js` 从模板与模块生成，不允许人工编辑。

## 台账与状态文档

实现与证据通过后由 Master 更新 `requirements/PRODUCT_REQUIREMENTS_LEDGER.json`、`PROJECT_STATUS.json`、七份生成报告和三份中文日志。
