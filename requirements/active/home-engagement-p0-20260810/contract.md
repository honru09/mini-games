# Home Engagement P0 冻结合同

1. 首页必须同时提供一眼可懂的三步下一步引导、推荐游戏入口和轻量成长目标。
2. 推荐只读取现有档案字段；缺失/访客/空档案使用本地化安全 fallback，不写入新权威字段。
3. 所有新增文字进入 zh-CN/en-US/uk-UA，动态游戏名仍由现有 `t()` 生成。
4. 按钮使用现有路由和 Profile/Shop 入口；不创建隐藏的自动开局、自动购买或服务器 mutation。
5. 样式必须继承双主题令牌，手机单列，控件保持 44px；reduced-motion 不依赖动画理解内容。
